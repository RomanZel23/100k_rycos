version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    ports:
      - "${API_HOST_PORT:-8008}:8000"
    environment:
      - NODE_ENV=production
      - PORT=8000
      - HOST=0.0.0.0
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET}
      - RYCOS_MQTT_HOST=${RYCOS_MQTT_HOST:-rycos.eu}
      - RYCOS_MQTT_PORT=${RYCOS_MQTT_PORT:-8883}
      - RYCOS_MQTT_USERNAME=${RYCOS_MQTT_USERNAME}
      - RYCOS_MQTT_PASSWORD=${RYCOS_MQTT_PASSWORD}
      - RYCOS_DEFAULT_DISPLAY_ID=${RYCOS_DEFAULT_DISPLAY_ID}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - coolify
    extra_hosts:
      - "host.docker.internal:host-gateway"

  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - RYCOS_MQTT_HOST=${RYCOS_MQTT_HOST:-rycos.eu}
      - RYCOS_MQTT_PORT=${RYCOS_MQTT_PORT:-8883}
      - RYCOS_MQTT_USERNAME=${RYCOS_MQTT_USERNAME}
      - RYCOS_MQTT_PASSWORD=${RYCOS_MQTT_PASSWORD}
      - RYCOS_DEFAULT_DISPLAY_ID=${RYCOS_DEFAULT_DISPLAY_ID}
    networks:
      - coolify
    extra_hosts:
      - "host.docker.internal:host-gateway"

  customer-web:
    build:
      context: .
      dockerfile: apps/customer-web/Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-https://100k-api.rycos.eu}
        - NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL:-wss://100k-api.rycos.eu/v1/ws}
    restart: unless-stopped
    ports:
      - "${WEB_HOST_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-https://100k-api.rycos.eu}
      - NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL:-wss://100k-api.rycos.eu/v1/ws}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - coolify

networks:
  coolify:
    external: true
