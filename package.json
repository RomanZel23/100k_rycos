{
  "name": "100k-rycos",
  "version": "2.1.0",
  "private": true,
  "description": "High-performance restaurant ordering, realtime KDS and RYCOS fiscalization system",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
